// Library for parsing SVG files along with tinyxml2

#ifndef INPUT_H_
#define INPUT_H_

using namespace std;

// Parse a SVG file and add the parsed data to the game object
void parse_svg_file(string filePath, Game *game);

#endif /* INPUT_H_ */
